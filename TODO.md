# TODO - Tasks before report + repo submission
## Report

### I.3
- Add appendix with transcript extracts showing that the training broke the conversational output of the model relative to the base model.
    - Generate transcripts for this on my personal VM.
- Change axis labels for mean reward plots to $\bar{r}$.
- generate the I.1 baseline plots as one figure with two plots side by side
- fix the readability of the I.3 3-abreast plots. Change the plotting code to plot the comparative KL+mean reward curves as one fig with two plots. Make the diagnostic plot independent.
- plot the Advantage variance curve as the main diagnostic plot (as mentioned in the coursework markdown), if possible. if not possible, justify why this cannot be done.
NB: Don't worry about page limits for now

- get the missing eval scores for the appendix eval table


## Repo (tpu-2026-fork)

