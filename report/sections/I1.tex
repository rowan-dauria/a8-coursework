\subsection{I.1 Reproducing the Baseline}

\noindent
I reproduced the baseline from the completed W\&B run \texttt{jgs4c6kl} and the evaluation logs recorded for the same checkpoint.

\paragraph{Run identity.}
\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{}L{0.32\linewidth}L{0.58\linewidth}@{}}
\toprule
Item & Value \\
\midrule
Code commit & \texttt{7e696c428687bc083604690f5f51009da6abb6d9} on branch \texttt{baseline-fls} \\
W\&B run & \href{https://wandb.ai/felsomoye-university-of-cambridge/tunix/runs/jgs4c6kl}{\texttt{jgs4c6kl}} \\
Wall-clock time & 4h 41m 36s \\
GRPO steps completed & 3364 (full schedule: $3738 \times 0.9 = 3364$) \\
\bottomrule
\end{tabular}
\caption{Baseline run identity.}
\label{tab:baseline-metadata}
\end{table}

\paragraph{Baseline patches.}
The baseline did not run as shipped because TFDS/GSM8K loading failed with a protobuf \texttt{FieldDescriptor.label} error, caused by a \texttt{protobuf} 7.x and \texttt{tensorflow-datasets} 4.9.9 compatibility issue. The fix was to pin \texttt{protobuf==6.31.1} in \texttt{requirements.txt}; this changed only dependency plumbing, not the model, reward functions, data split, or training hyperparameters.

\paragraph{Accuracy.}
\begin{table}[htbp]
\centering
\small
\begin{tabular}{@{}L{0.40\linewidth}L{0.28\linewidth}L{0.12\linewidth}L{0.12\linewidth}@{}}
\toprule
Model/checkpoint & Split & Seed & Exact accuracy \\
\midrule
Base \texttt{gemma-3-1b-it} & GSM8K test, first 64 & 42 & 33/64 = 51.56\% \\
Baseline LoRA GRPO, step 3364 & GSM8K test, first 64 & 42 & 2/64 = 3.12\% \\
\bottomrule
\end{tabular}
\caption{Base-model and baseline-checkpoint GSM8K accuracy on the official test split (seed-42 shuffle, first 64 examples, greedy decoding).}
\label{tab:baseline-accuracy}
\end{table}

\noindent
The LoRA checkpoint is substantially worse than the base model, indicating a training failure rather than a checkpoint-restore bug.

\paragraph{Training curves.}

\begin{figure}[htbp]
\optionalfigure{../report-materials/data-and-figs/baseline_mean_reward_curve.pdf}{insert baseline held-out eval reward vs.\ GRPO step}
\caption{Baseline held-out eval reward (mean per-rollout total reward, summed over the four reward terms) over GRPO training, evaluated on the held-out eval set every 64 steps.}
\label{fig:baseline-reward}
\end{figure}

\begin{figure}[htbp]
\optionalfigure{../tpu-2026-fork/report_assets/baseline_kl_curve.pdf}{insert baseline KL curve, $\mathrm{KL}(\pi_\theta\|\pi_{\mathrm{ref}})$ vs.\ GRPO step}
\caption{Baseline reference-KL $\mathrm{KL}(\pi_\theta\|\pi_{\mathrm{ref}})$ during GRPO training.}
\label{fig:baseline-kl}
\end{figure}

\noindent
The held-out eval reward rises early, peaking around 6.8 near step 460, then declines through an oscillating descent to about $-1$ by step $\sim$2750 and flatlines there. The KL remains around 0.5 for most of training, with spikes near steps 2500 and 3300. The policy learned reward-correlated behaviour early but subsequently drifted away from the reference without preserving GSM8K accuracy.
